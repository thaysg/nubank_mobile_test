import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFF959be1);
  static const primaryDark = Color(0xFF4A0A8E);
  static const primaryLight = Color(0xFFF7E7FF);
  static const scaffoldBackgroundLight = Color(0xFFFBFBFC);
  static const backgroundLight = Color(0xFFFEFEFE);
  static const backgroundDark = Color(0xFF121212);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color.fromARGB(235, 36, 42, 50);
  static const textSecondary = Color(0xFF666666);
  static const error = Color(0xFFE53E3E);
  static const warning = Color(0xFFD69E2E);
  static const success = Color(0xFF2E7D32);
  static const divider = Color(0xFFE0E0E0);

  // Favicon Avatar Colors
  static const blue = Color(0xFF3182CE);
  static const green = Color(0xFF38A169);
  static const yellow = Color(0xFFD69E2E);
  static const purple = Color(0xFF805AD5);

  static const colorsList = [
    AppColors.primary,
    AppColors.blue,
    AppColors.green,
    AppColors.yellow,
    AppColors.error,
    AppColors.purple,
  ];
}
